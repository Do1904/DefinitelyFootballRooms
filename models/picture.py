from app.models.abstract import AbstractModel


class PictureModel(AbstractModel):
    def __init__(self, config):
        super(PictureModel, self).__init__(config)

    def test_print(picture):
        pic = picture
        return pic